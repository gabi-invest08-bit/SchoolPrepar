<form method="post" action="{{ path('app_mentor_control_delete', {'id': mentor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mentor.id) }}">
    <button class="btn">Delete</button>
</form>
