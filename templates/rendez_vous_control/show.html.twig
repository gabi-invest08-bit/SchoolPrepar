{% extends 'base.html.twig' %}

{% block title %}RendezVous{% endblock %}

{% block body %}
    <h1>RendezVous</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rendez_vou.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ rendez_vou.date ? rendez_vou.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ rendez_vou.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rendez_vous_control_index') }}">back to list</a>

    <a href="{{ path('app_rendez_vous_control_edit', {'id': rendez_vou.id}) }}">edit</a>

    {{ include('rendez_vous_control/_delete_form.html.twig') }}
{% endblock %}
