<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <meta name="description" content="">
  <meta name="author" content="">
  <link href="https://fonts.googleapis.com/css?family=Montserrat:100,200,300,400,500,600,700,800,900" rel="stylesheet">

  <title>SchoolPrepar</title>

  {# Bootstrap via CDN (les fichiers vendor ne sont pas dans public/) #}
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/css/bootstrap.min.css" rel="stylesheet">

  {# CSS du template — tous dans public/assets/css/ #}
  <link rel="stylesheet" href="{{ asset('assets/css/fontawesome.css') }}">
  <link rel="stylesheet" href="{{ asset('assets/css/templatemo-grad-school.css') }}">
  <link rel="stylesheet" href="{{ asset('assets/css/owl.css') }}">
  <link rel="stylesheet" href="{{ asset('assets/css/lightbox.css') }}">
</head>

<body>

  <!-- ===== HEADER ===== -->
  <header class="main-header clearfix" role="header">
    <div class="logo">
      <a href="#top"><em>School</em> Prepar</a>
    </div>
    <a href="#menu" class="menu-link"><i class="fa fa-bars"></i></a>
    <nav id="menu" class="main-nav" role="navigation">
      <ul class="main-menu">
        <li><a href="#section1">Home</a></li>
        <li><a href="#section2">About Us</a></li>
        <li><a href="#section4">Courses</a></li>
        <li><a href="#section4b">Establishments</a></li>
        <li><a href="#section5">Mentors</a></li>
        <li><a href="#section6">Contact</a></li>
      </ul>
    </nav>
  </header>

  <!-- ===== MAIN BANNER ===== -->
  <section class="section main-banner" id="top" data-section="section1">
    <video autoplay muted loop id="bg-video">
      <source src="{{ asset('assets/images/course-video.mp4') }}" type="video/mp4" />
    </video>

    <div class="video-overlay header-text">
      <div class="caption">
        <h6>Graduate School of Management</h6>
        <h2><em>Your</em> Classroom</h2>
        <div class="main-button">
          <div class="scroll-to-section"><a href="#section2">Discover more</a></div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== FEATURES ===== -->
  <section class="features">
    <div class="container">
      <div class="row">
        <div class="col-lg-4 col-12">
          <div class="features-post">
            <div class="features-content">
              <div class="content-show">
<h4><i class="fa fa-pencil"></i> {{ stats.filieresCount }} Filières</h4>
              </div>
              <div class="content-hide">
                <p>Curabitur id eros vehicula, tincidunt libero eu, lobortis mi. In mollis eros a posuere imperdiet. Donec maximus elementum ex. Cras convallis ex rhoncus, laoreet libero eu, vehicula libero.</p>
                <p class="hidden-sm">Curabitur id eros vehicula, tincidunt libero eu, lobortis mi. In mollis eros a posuere imperdiet.</p>
                <div class="scroll-to-section"><a href="#section2">More Info.</a></div>
              </div>
            </div>
          </div>
        </div>
        <div class="col-lg-4 col-12">
          <div class="features-post second-features">
            <div class="features-content">
              <div class="content-show">
<h4><i class="fa fa-graduation-cap"></i> {{ stats.etablissementsCount }} Établissements</h4>
              </div>
              <div class="content-hide">
                <p>Curabitur id eros vehicula, tincidunt libero eu, lobortis mi. In mollis eros a posuere imperdiet. Donec maximus elementum ex. Cras convallis ex rhoncus, laoreet libero eu, vehicula libero.</p>
                <p class="hidden-sm">Curabitur id eros vehicula, tincidunt libero eu, lobortis mi. In mollis eros a posuere imperdiet.</p>
                <div class="scroll-to-section"><a href="#section3">Details</a></div>
              </div>
            </div>
          </div>
        </div>
        <div class="col-lg-4 col-12">
          <div class="features-post third-features">
            <div class="features-content">
              <div class="content-show">
<h4><i class="fa fa-book"></i> {{ stats.mentorsCount }} Mentors</h4>
              </div>
              <div class="content-hide">
                <p>Curabitur id eros vehicula, tincidunt libero eu, lobortis mi. In mollis eros a posuere imperdiet. Donec maximus elementum ex. Cras convallis ex rhoncus, laoreet libero eu, vehicula libero.</p>
                <p class="hidden-sm">Curabitur id eros vehicula, tincidunt libero eu, lobortis mi. In mollis eros a posuere imperdiet.</p>
                <div class="scroll-to-section"><a href="#section4">Read More</a></div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== WHY US ===== -->
  <section class="section why-us" data-section="section2">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <div class="section-heading">
            <h2>Why choose SchoolPrepar?</h2>
          </div>
        </div>
        <div class="col-md-12">
          <div id="tabs">
            <ul>
              <li><a href="#tabs-1">Best Education</a></li>
              <li><a href="#tabs-2">Top Management</a></li>
              <li><a href="#tabs-3">Quality Meeting</a></li>
            </ul>
            <section class="tabs-content">
              <article id="tabs-1">
                <div class="row">
                  <div class="col-md-6">
                    <img src="{{ asset('assets/images/choose-us-image-01.png') }}" alt="Nos Établissements">
                  </div>
                  <div class="col-md-6">
                    <h4>Nos Établissements</h4>
                    <p>{{ stats.etablissementsCount }} établissements disponibles.</p>
                    {% if etablissements %}
                    <ul>
                    {% for etablissement in etablissements %}
                      <li>{{ etablissement.nom }} - {{ etablissement.ville }} ({{ etablissement.pays }})</li>
                    {% endfor %}
                    </ul>
                    {% else %}
                    <p>Aucun établissement pour le moment.</p>
                    {% endif %}
                  </div>
                </div>
              </article>
              <article id="tabs-2">
                <div class="row">
                  <div class="col-md-6">
                    <img src="{{ asset('assets/images/choose-us-image-02.png') }}" alt="Nos Filières">
                  </div>
                  <div class="col-md-6">
                    <h4>Nos Filières</h4>
                    <p>{{ stats.filieresCount }} filières disponibles.</p>
                    {% if filieres %}
                    <ul>
                    {% for filiere in filieres %}
                      <li>{{ filiere.nom }}<br><small>{{ filiere.description|slice(0,100) ~ '...' }}</small></li>
                    {% endfor %}
                    </ul>
                    {% else %}
                    <p>Aucune filière pour le moment.</p>
                    {% endif %}
                  </div>
                </div>
              </article>
              <article id="tabs-3">
                <div class="row">
                  <div class="col-md-6">
                    <img src="{{ asset('assets/images/choose-us-image-03.png') }}" alt="Nos Mentors">
                  </div>
                  <div class="col-md-6">
                    <h4>Nos Mentors</h4>
                    <p>{{ stats.mentorsCount }} mentors experts.</p>
                    {% if mentors %}
                    <ul>
                    {% for mentor in mentors %}
                      <li>{{ mentor.profession }}<br><small>{{ mentor.bio|slice(0,100)|default('Bio non disponible') ~ '...' }}</small></li>
                    {% endfor %}
                    </ul>
                    {% else %}
                    <p>Aucun mentor pour le moment.</p>
                    {% endif %}
                  </div>
                </div>
              </article>
            </section>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== COMING SOON / REGISTER ===== -->
  <section class="section coming-soon" data-section="section3">
    <div class="container">
      <div class="row">
        <div class="col-md-7 col-xs-12">
          <div class="continer centerIt">
            <div>
              <h4>Take <em>any online course</em> and win $326 for your next class</h4>
              <div class="counter">
                <div class="days">
                  <div class="value">00</div>
                  <span>Days</span>
                </div>
                <div class="hours">
                  <div class="value">00</div>
                  <span>Hours</span>
                </div>
                <div class="minutes">
                  <div class="value">00</div>
                  <span>Minutes</span>
                </div>
                <div class="seconds">
                  <div class="value">00</div>
                  <span>Seconds</span>
                </div>
              </div>
            </div>
          </div>
        </div>
        <div class="col-md-5">
          <div class="right-content">
            <div class="top-content">
              <h6>Register your free account and <em>get immediate</em> access to online courses</h6>
            </div>
            {# Formulaire d'inscription — adapte l'action si tu as une route dédiée #}
            <form id="register-form" action="{{ path('app_user_control_new') }}" method="post">
              <input type="hidden" name="_token" value="{{ csrf_token('register') }}">
              <div class="row">
                <div class="col-md-12">
                  <fieldset>
                    <input name="name" type="text" class="form-control" id="reg-name" placeholder="Your Name" required>
                  </fieldset>
                </div>
                <div class="col-md-12">
                  <fieldset>
                    <input name="email" type="email" class="form-control" id="reg-email" placeholder="Your Email" required>
                  </fieldset>
                </div>
                <div class="col-md-12">
                  <fieldset>
                    <input name="phone" type="tel" class="form-control" id="reg-phone" placeholder="Your Phone Number" required>
                  </fieldset>
                </div>
                <div class="col-md-12">
                  <fieldset>
                    <button type="submit" class="button">Get it now</button>
                  </fieldset>
                </div>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== COURSES ===== -->
  <section class="section courses" data-section="section4">
    <div class="container-fluid">
      <div class="row">
        <div class="col-md-12">
          <div class="section-heading">
            <h2>Choose Your Course</h2>
          </div>
        </div>
        <div class="owl-carousel owl-theme">

          {% for filiere in filieres %}
          <div class="item">
            <img src="{{ asset('assets/images/courses-' ~ ((loop.index0 % 5) + 1) ~ '.jpg') }}" alt="Filière {{ filiere.nom }}">
            <div class="down-content">
              <h4>{{ filiere.nom }}</h4>
              <p>{{ filiere.description|length > 0 ? filiere.description|slice(0, 150) ~ '...' : 'Description à venir.' }}</p>
              <div class="author-image">
                <img src="{{ asset('assets/images/author-' ~ ((loop.index0 % 5) + 1) ~ '.png') }}" alt="">
              </div>
              <div class="text-button-pay">
                <a href="{{ path('app_filiere_control_show', {'id': filiere.id}) }}">Voir <i class="fa fa-angle-double-right"></i></a>
              </div>
            </div>
          </div>
          {% else %}
          <div class="item">
            <h4 style="text-align: center; padding: 50px;">Aucune filière disponible pour le moment. <a href="{{ path('app_filiere_control_new') }}">Créez la première !</a></h4>
          </div>
          {% endfor %}

        </div>
      </div>
    </div>
  </section>

  <!-- ===== ESTABLISHMENTS ===== -->
  <section class="section establishments" data-section="section4b">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <div class="section-heading">
            <h2>Our <em>Establishments</em></h2>
          </div>
        </div>
      </div>
      <div class="row establishments-row">
        {% if etablissements %}
          {% for etablissement in etablissements %}
          <div class="col-lg-4 col-md-6 col-sm-12 mb-4">
            <div class="establishment-card">
              <div class="establishment-content">
                <h3>{{ etablissement.nom }}</h3>
                <p class="location"><i class="fa fa-map-marker"></i> {{ etablissement.ville }}, {{ etablissement.pays }}</p>
                <p class="description">{{ etablissement.description|default('Établissement de formation de qualité') }}</p>
                <div class="establishment-footer">
                  <a href="{{ path('app_etablissement_contol_show', {'id': etablissement.id}) }}" class="btn-visit">View Details</a>
                </div>
              </div>
            </div>
          </div>
          {% endfor %}
        {% else %}
          <div class="col-md-12">
            <p style="text-align: center; color: #fff; font-size: 18px;">No establishments available at the moment.</p>
          </div>
        {% endif %}
      </div>
    </div>
  </section>

  <!-- ===== VIDEO ===== -->
  <section class="section video" data-section="section5">
    <div class="container">
      <div class="row">
        <div class="col-md-6 align-self-center">
          <div class="left-content">
            <span>our presentation is for you</span>
            <h4>Watch the video to learn more <em>about SchoolPrepar</em></h4>
            <p>Découvrez nos {{ stats.mentorsCount }} mentors experts.</p>
                    {% if mentors %}
                    <ul>
                    {% for mentor in mentors %}
                      <li><strong>{{ mentor.profession }}</strong> - {{ mentor.disponibilite|default('Disponible') }}</li>
                    {% endfor %}
                    </ul>
                    {% else %}
                    <p>Rejoignez nos mentors bientôt !</p>
                    {% endif %}
            <br><br>Suspendisse tincidunt, magna ut finibus rutrum, libero dolor euismod odio, nec interdum quam felis non ante.</p>
            <div class="main-button">
              <a href="{{ path('app_mentor_control_index') }}">Voir les Mentors</a>
            </div>
          </div>
        </div>
        <div class="col-md-6">
          <article class="video-item">
            <div class="video-caption">
              <h4>SchoolPrepar Platform</h4>
            </div>
            <figure>
              <a href="https://www.youtube.com/watch?v=r9LtOG6pNUw" target="_blank" class="play">
                <img src="{{ asset('assets/images/main-thumb.png') }}" alt="Video thumbnail">
              </a>
            </figure>
          </article>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== CONTACT ===== -->
  <section class="section contact" data-section="section6">
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <div class="section-heading">
            <h2>Let's Keep In Touch</h2>
          </div>
        </div>
        <div class="col-md-6">
          {# Formulaire de contact — adapte l'action avec ta route si elle existe #}
          <form id="contact-form" action="{{ path('app_message_control_new') }}" method="post">
            <input type="hidden" name="_token" value="{{ csrf_token('contact') }}">
            <div class="row">
              <div class="col-md-6">
                <fieldset>
                  <input name="name" type="text" class="form-control" id="contact-name" placeholder="Your Name" required>
                </fieldset>
              </div>
              <div class="col-md-6">
                <fieldset>
                  <input name="email" type="email" class="form-control" id="contact-email" placeholder="Your Email" required>
                </fieldset>
              </div>
              <div class="col-md-12">
                <fieldset>
                  <textarea name="message" rows="6" class="form-control" id="contact-message" placeholder="Your message..." required></textarea>
                </fieldset>
              </div>
              <div class="col-md-12">
                <fieldset>
                  <button type="submit" class="button">Send Message Now</button>
                </fieldset>
              </div>
            </div>
          </form>
        </div>
        <div class="col-md-6">
          <div id="map">
            <iframe
              src="https://maps.google.com/maps?q=Lom%C3%A9,+Togo&t=&z=13&ie=UTF8&iwloc=&output=embed"
              width="100%"
              height="422px"
              frameborder="0"
              style="border:0"
              allowfullscreen
              loading="lazy"
              title="Map location">
            </iframe>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== FOOTER ===== -->
  <footer>
    <div class="container">
      <div class="row">
        <div class="col-md-12">
          <p>
            <i class="fa fa-copyright"></i> Copyright 2024 by SchoolPrepar &nbsp;|&nbsp;
            Design: <a href="https://templatemo.com" rel="sponsored" target="_blank">TemplateMo</a>
          </p>
        </div>
      </div>
    </div>
  </footer>

  <!-- ===== SCRIPTS ===== -->
  {# jQuery et Bootstrap via CDN (les fichiers vendor ne sont pas dans public/) #}
  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>

  {# JS du template — tous dans public/assets/js/ #}
  <script src="{{ asset('assets/js/isotope.min.js') }}"></script>
  <script src="{{ asset('assets/js/owl-carousel.js') }}"></script>
  <script src="{{ asset('assets/js/lightbox.js') }}"></script>
  <script src="{{ asset('assets/js/tabs.js') }}"></script>
  <script src="{{ asset('assets/js/video.js') }}"></script>
  <script src="{{ asset('assets/js/slick-slider.js') }}"></script>
  <script src="{{ asset('assets/js/custom.js') }}"></script>

  <script>
    $('.nav li:first').addClass('active');

    var showSection = function (section, isAnimate) {
      var direction     = section.replace(/#/, ''),
          reqSection    = $('.section').filter('[data-section="' + direction + '"]'),
          reqSectionPos = reqSection.offset().top - 0;

      if (isAnimate) {
        $('body, html').animate({ scrollTop: reqSectionPos }, 800);
      } else {
        $('body, html').scrollTop(reqSectionPos);
      }
    };

    var checkSection = function () {
      $('.section').each(function () {
        var $this      = $(this),
            topEdge    = $this.offset().top - 80,
            bottomEdge = topEdge + $this.height(),
            wScroll    = $(window).scrollTop();

        if (topEdge < wScroll && bottomEdge > wScroll) {
          var currentId = $this.data('section'),
              reqLink   = $('a').filter('[href*=\\#' + currentId + ']');
          reqLink.closest('li').addClass('active').siblings().removeClass('active');
        }
      });
    };

    $('.main-menu, .scroll-to-section').on('click', 'a', function (e) {
      if ($(e.target).hasClass('external')) { return; }
      e.preventDefault();
      $('#menu').removeClass('active');
      showSection($(this).attr('href'), true);
    });

    $(window).scroll(function () {
      checkSection();
    });
  </script>

</body>
</html>
