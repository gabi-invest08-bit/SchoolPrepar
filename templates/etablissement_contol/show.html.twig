{% extends 'base.html.twig' %}

{% block title %}Etablissement{% endblock %}

{% block body %}
    <h1>Etablissement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etablissement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ etablissement.nom }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ etablissement.ville }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ etablissement.pays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etablissement_contol_index') }}">back to list</a>

    <a href="{{ path('app_etablissement_contol_edit', {'id': etablissement.id}) }}">edit</a>

    {{ include('etablissement_contol/_delete_form.html.twig') }}
{% endblock %}
